{{template "header" .}}
        <h3> Dashboard </h3>
        <a href="/workout/">New Workout</a>
        <ul>
            {{range .}}
            <li><a href="/workout/{{.}}">{{.}}</a></li>
            {{end}}
        </ul>
{{template "footer" .}}
